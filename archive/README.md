# archive/

These are leftovers from a much earlier, unrelated version of Council (a Rust
scaffold). They are kept here for history only and are **not** part of the
current site build. Nothing under `archive/` is referenced by `flake.nix` or the
deploy workflow.
